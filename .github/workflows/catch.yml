name: catch

on: [push, pull_request]

env:
  BUILD_TYPE: Release

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        # os: [ubuntu-latest, macOS-latest]

    steps:
      - uses: actions/checkout@v2

      - name: Create Build Environment
        # working-directory: ${{runner.workspace}}/autode/ext/
        
        run: echo $PWD && echo ${{runner.workspace}} && ls ${{runner.workspace}} && cmake -E make_directory build

      - name: Configure CMake
        shell: bash
        working-directory: ${{runner.workspace}}/autode/ext/build
        run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE

      - name: Build
        working-directory: ${{runner.workspace}}/autode/ext/build
        shell: bash
        run: cmake --build . --config $BUILD_TYPE

      - name: Test
        working-directory: ${{runner.workspace}}/autode/ext/build
        shell: bash
        run: ctest -C $BUILD_TYPE --extra-verbose
